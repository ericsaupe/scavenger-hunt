<%= render(PageHeaderComponent.new(title: @hunt.name, subtitle: "Change some settings or just leave the defaults to get started!")) %>

<div class="mx-4 space-y-8 sm:space-y-5">
  <%= render partial: 'form', locals: { hunt: @hunt } %>
</div>
